# Relatório — TASK-051 · Adicionar aria-expanded ao toggle do drawer mobile

**Data:** 2026-05-22
**Classe:** D — correção pequena / acessibilidade / UI
**Status:** **concluída**
**Origem:** Achado H1 da TASK-048 (`docs/relatorios/2026-05-22-TASK-048.md`)

---

## 1. Resumo executivo

Resolvido o achado **H1 da TASK-048** — toggle do drawer mobile sem `aria-expanded`. Edição cirúrgica em `src/components/map/ProjectMap.tsx`: 3 atributos ARIA no botão (`aria-expanded`, `aria-controls`, `aria-label` dinâmico) + 1 `id` estável no `<aside>`. Total ~5 linhas em `src/`.

**Validação Playwright (mini sessão, viewport mobile 375×812):** 8/8 verificações ✅. Estado `aria-expanded` alterna corretamente entre `"false"` (inicial) e `"true"` (pós-click); `aria-label` muda dinamicamente; `#project-layout-drawer` presente no DOM.

**Estado preservado:** `tsc 0 erros` · `vitest 826/826` · `src/lib/**` intocado · dependências intocadas.

## 2. O que foi feito

### Arquivos modificados

| Arquivo | Linhas | Mudança |
|---|---|---|
| `src/components/map/ProjectMap.tsx` | 1640-1647 (botão) + 1687 (aside) | Adicionados `aria-expanded={sidebarOpen}`, `aria-controls="project-layout-drawer"`, `aria-label` dinâmico, `id="project-layout-drawer"` |

### Arquivos criados

| Arquivo | Conteúdo |
|---|---|
| `tasks/TASK-051-aria-expanded-drawer-mobile.md` | Task file Classe D |
| `docs/relatorios/2026-05-22-TASK-051.md` | Este relatório |
| `docs/relatorios/evidencias/2026-05-22-TASK-051/task-051-01-drawer-fechado-aria-expanded-false.png` | Screenshot estado inicial |
| `docs/relatorios/evidencias/2026-05-22-TASK-051/task-051-02-drawer-aberto-aria-expanded-true.png` | Screenshot pós-click |

## 3. Validação Playwright

Sequência (viewport 375×812):

| Passo | Ação | Resultado |
|---|---|---|
| 1 | `browser_resize` 375×812 | OK |
| 2 | `browser_navigate` → Projeto A | UI carregou; 0 errors no console |
| 3 | `browser_evaluate` estado inicial | botaoExiste=true; ariaExpanded=`"false"`; ariaControls=`"project-layout-drawer"`; ariaLabel=`"Abrir painel de layout do projeto"`; drawerExiste=true (tag `ASIDE`) |
| 4 | `browser_take_screenshot` estado fechado | `task-051-01-drawer-fechado-aria-expanded-false.png` |
| 5 | `browser_evaluate` click no toggle | clicked=true |
| 6 | `browser_evaluate` estado pós-click | ariaExpanded=`"true"`; ariaLabel=`"Fechar painel de layout do projeto"`; ariaControls=preservado |
| 7 | `browser_take_screenshot` estado aberto | `task-051-02-drawer-aberto-aria-expanded-true.png` |
| 8 | `browser_close` | sessão encerrada |

**Resultado:** 8/8 verificações ✅.

## 4. Estado da suíte

| Verificação | Antes | Após |
|---|---|---|
| `npx tsc --noEmit` | 0 erros | **0 erros** ✓ |
| `npx vitest run` | 826/826 | **826/826** ✓ (sem regressão; mudança em JSX não afeta testes de domínio) |
| `node scripts/ai/__tests__/run-all.mjs` | 20/20 | Não re-executado — `scripts/ai/**` não tocado; baseline preservado |
| `src/lib/**` modificado | — | **Não** ✓ |
| Mapbox/canvas/layers tocados | — | **Não** ✓ (apenas `<aside>` + `<button>` ARIA) |
| Dependência npm nova | — | **Não** ✓ |
| ADRs / premissas alteradas | — | **Não** ✓ |
| `docs/metodologia/01-regras-bloqueantes.md` alterado | — | **Não** ✓ |

## 5. Achado H1 da TASK-048 — resolução

**Origem do achado** (`docs/relatorios/2026-05-22-TASK-048.md` Seção 5):
> H1 — Toggle do drawer mobile sem `aria-expanded`. Severidade: menor (acessibilidade). Diagnóstico: `browser_evaluate` em ambos os estados retornou `toggleAriaExpanded: null`. Funcionalidade: funciona — drawer abre, conteúdo rolável, overlay fecha. Apenas a comunicação semântica está incompleta. Recomendação: task D futura — adicionar `aria-expanded={drawerOpen}`. Escopo ≤ 5 linhas em `ProjectMap.tsx`.

**Status pós-TASK-051:** ✅ **RESOLVIDO**.

- Comunicação semântica completa: `aria-expanded`, `aria-controls`, `aria-label` dinâmico.
- Tamanho real: ~5 linhas em `src/components/map/ProjectMap.tsx` (estimativa do H1 confirmada).

**Histórico preservado:** menção ao H1 na TASK-048 NÃO foi apagada — mantém-se como rastro histórico do ciclo de descoberta → resolução.

## 6. Conformidade com regras

| Regra | Status |
|---|---|
| Catálogo read-only (`CLAUDE.md`) | ✅ não tocado |
| Nenhuma lógica de domínio em UI | ✅ apenas atributos ARIA refletindo estado existente |
| Orquestrador único | ✅ não tocado |
| Invariantes tsc/vitest preservadas | ✅ 0 erros / 826 testes |
| Fluxo Classe D | ✅ `/iniciar-task → /planejar → /implementar → tsc + vitest → /fechar-task` (este relatório) |
| Critério Classe D: ≤ 20 linhas em `src/` | ✅ ~5 linhas reais |
| Critério Classe D: bug com causa-raiz identificada | ✅ achado H1 da TASK-048 |

## 7. Pendências abertas

Nenhuma. H1 totalmente resolvido. Suíte preservada. Sem regressão observada.

## 8. Recomendação para commit

Recomendo commit isolado de TASK-051 com 6 arquivos:
- `src/components/map/ProjectMap.tsx` (modificado)
- `tasks/TASK-051-aria-expanded-drawer-mobile.md` (criado)
- `docs/relatorios/2026-05-22-TASK-051.md` (criado)
- `docs/relatorios/evidencias/2026-05-22-TASK-051/` (2 PNGs criados)
- `tasks/backlog.md` (entrada formal + remoção da bullet "Próximas tarefas sugeridas")
- `tasks/TASK-024-mapa-mestre-tasks.md` (hunk H1 resolvido)

Mensagem sugerida: `fix(a11y): close TASK-051 add aria-expanded to mobile drawer toggle`

## 9. Próximas tasks naturais (não criadas)

- TASK-001 — Diagnóstico formal do software atual (Classe A, bloqueante de E08).
- TOOL-003 — Captura de `response.usage` real da Responses API (Classe B, descoberto em TOOL-002).
- Pressão real por derivação `cumPrincipalHfM` (Classe A, pendência E03).
- Validação RT do PDF como proposta completa (Classe E).
- Comparação com projeto histórico real (Classe E).
- Auditoria geral de a11y (Classe B/E, se desejado).
