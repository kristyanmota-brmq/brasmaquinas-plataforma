# Resumo de Sessão — TASK-058 — Correções cirúrgicas do diagnóstico especialista

**Data:** 2026-06-11
**Testes ao início:** 951/951 · **ao final:** 953/953 (+2 T58)
**TypeScript erros:** 0 · Tooling: 37/37

---

## Contexto

O usuário pediu crítica de especialista em irrigação sobre o gerador. Foi executada auditoria dupla: visual ao vivo via Chrome (projeto Fazenda do Paulo) + metodológica via subagent (`docs/relatorios/2026-06-11-diagnostico-especialista-irrigacao.md`). Das 6 críticas ranqueadas, as prioridades #1 e #2 (XS) foram autorizadas ("Prossiga") e implementadas nesta task.

## O que foi feito

**Fix 1 — ADR-002 em `hazenWilliams.ts`:** `selectDiameter` usava diâmetro nominal nos cálculos HW/velocidade (subestimando hf em ≈47% no DN50). Agora usa `diametroInternoMm ?? diametroMm` nos 4 pontos. Violação latente (sem consumidor de produção; `selectTubo` já estava correto), corrigida na API pública.

**Fix 2 — feedback do PDF 422 em `ProjectMap.tsx`:** com todos os blockers classificados rt-pending (azuis), o clique no PDF retornava 422 e a UI não mostrava NADA (regressão B-05/W-08 — o painel de bloqueio era aninhado no bloco vermelho data-block). Novo banner "PDF bloqueado pela governança" independente da partição, com detalhes de segmentos inválidos migrados para ele.

## Testes novos

| Arquivo | Δ | Cobertura |
|---|---|---|
| `laterais.test.ts` | +2 (T58-1, T58-2) | hf com Øint vs nominal; fallback quando `diametroInternoMm` ausente |

## Verificação visual (browser real)

Projeto Fazenda do Paulo (2 blockers rt-pending, 0 data-block): clique no PDF → `POST /api/projetos/.../pdf 422` → banner vermelho "PDF BLOQUEADO PELA GOVERNANÇA — há 2 pendência(s) ativa(s)" renderizado acima do painel azul. Antes da fix: nenhum feedback.

## Invariantes

- [x] tsc 0 · vitest 953/953 · tooling 37/37
- [x] Domínio fora da UI · catálogo intocado · TECH-053-01 ATIVO · contrato `blockers: string[]` intocado

## Pendências (diagnóstico, fora de escopo)

Ver `2026-06-11-diagnostico-especialista-irrigacao.md` §5 — itens #3-#6 (motor no fluxo de traçado, agronomia, catálogo PN80/custos, setorização por demanda) aguardam decisão RT/produto.

## Próximos passos

1. **TASK-053-valves** (Fase 2 do roadmap) — mitiga TECH-053-01
2. **Decisão RT** sobre as críticas 1, 4 e 5 do diagnóstico
