# Relatório — TASK-004B · Pressão real por derivação / cumPrincipalHfM

**Data:** 2026-05-22
**Classe:** A — motor hidráulico
**Status:** **implementação concluída** (sem commit/push — aguarda aprovação humana)
**Predecessor:** TASK-004 (concluída 2026-05-19; ADR-008 — Alternativa C reservada para tarefa futura)
**Veredito GPT:** `aprovado_com_ajustes` · 2 blockers · 0/7 invariantes violadas
**Decisão humana:** `aprovado_com_ajustes` (Opção A para ambos blockers) · `ai/decision-log.md` 2026-05-22T21:02:18-03:00 · hash `35c7ba49...59efa`

---

## 1. Resumo executivo

TASK-004B entrega a **Alternativa C da ADR-008** explicitamente reservada para tarefa futura: substituição do cálculo conservador de pressão em ramais e laterais (HMT como teto único) por **pressão real por derivação**:

```
pressaoOperacionalMaxMca = HMT − adutoraHfM − cumPrincipalHfM(até derivação)
```

Mudança cirúrgica em [`src/lib/layout/hydraulic-sizing.ts`](../../src/lib/layout/hydraulic-sizing.ts) (~30 linhas adicionadas): 2 campos opcionais novos em `HydraulicSegment`, novo helper puro `derivePressureClassModel`, atualização de `annotatePressureClass`, propagação no solver, e detecção dinâmica em `modelLimitations.pressureClassModel`. 10 testes novos em [`src/lib/layout/__tests__/pressure-class.test.ts`](../../src/lib/layout/__tests__/pressure-class.test.ts).

**Resultado prático:** warnings espúrios `violation_conservative` em ramais/laterais com pressão real abaixo do PN são substituídos por classificação correta (`ok`); ramais/laterais com pressão real acima do PN são promovidos de warning conservador para `violation_confirmed` (blocker real). Comportamento de fallback preservado quando dados de derivação ausentes.

**Sem regressão:** 826/826 → 836/836 vitest (apenas adições); 0 erros tsc; 27/27 testes tooling. Nenhum teste em `integration.test.ts`, `bom.test.ts`, `pipeline-diagnostics.test.ts` precisou de adaptação — a propagação dos novos campos é aditiva e não rompe nenhum invariante existente.

**Produto exclusivamente em `src/lib/layout/hydraulic-sizing.ts`**. Nenhum arquivo em `src/lib/catalog/`, `src/lib/bom.ts`, `src/lib/pdf/`, `src/components/`, `src/app/`, `src/lib/layout/secondary-sizing.ts` ou geometria foi alterado. Mapa Mestre, premissas RT/campo, ADR-008 (sem emenda) e regras bloqueantes (sem `RB-09` nova) preservados intactos.

---

## 2. Fluxo TOOL-003 executado integralmente

```
/iniciar-task TASK-004B          [Context Gate aprovado pelo usuário]
   ↓
/planejar TASK-004B               [Plano detalhado aprovado pelo usuário]
   ↓
/handoff-claude-report TASK-004B  [ai/claude-report.md serializado com confirmação humana]
   ↓
/gpt-review TASK-004B             [Chamada real à Responses API (gpt-5.5)
                                   HTTP 200, 41,7s, veredito aprovado_com_ajustes,
                                   2 blockers TEC-004B-001 + MET-004B-001]
   ↓
Decisão humana                    [ai/decision-log.md — Opção A para ambos blockers
                                   hash gpt-review: 35c7ba49...59efa]
   ↓
/implementar TASK-004B            [Esta execução — 5 edits cirúrgicos + 10 testes]
   ↓
/fechar-task TASK-004B            [Próximo após aprovação humana de commit/push]
```

Primeira task fora de validação interna **a usar `/gpt-review` em produção e endereçar blockers do GPT via Opção A explícita no `decision-log.md`** — após TASK-001 que veio limpa (0 blockers).

---

## 3. Blockers do GPT Reviewer (endereçados)

### 3.1 TEC-004B-001 (técnico) — Opção A

> **GPT:** "A lógica proposta para `pressureClassModel` diz considerar `exact_per_derivation` quando todos os ramais/laterais tiverem `cumPrincipalHfM` definido, mas `annotatePressureClass` só usa o cálculo real quando `cumPrincipalHfM` E `adutoraHfM` estão ambos definidos. Ajuste recomendado: a detecção do modelo deve exigir ambos os campos para os segmentos `secondary`/`lateral`, ou documentar explicitamente que `adutoraHfM` é sempre populado no caminho do solver. Do contrário, o resultado pode declarar modelo exato enquanto alguns segmentos caem no fallback conservador."

**Resolução adotada:** Opção A — detecção exige **ambos** os campos. Implementado via helper puro novo:

```typescript
export function derivePressureClassModel(
  allSegments: readonly HydraulicSegment[],
): "hmt_conservative_inlet" | "exact_per_derivation" {
  const relevant = allSegments.filter(
    (s) => s.type === "secondary" || s.type === "lateral",
  );
  if (relevant.length === 0) return "hmt_conservative_inlet";
  const allHaveDerivationData = relevant.every(
    (s) => s.cumPrincipalHfM != null && s.adutoraHfM != null,
  );
  return allHaveDerivationData ? "exact_per_derivation" : "hmt_conservative_inlet";
}
```

Helper exportado e testado isoladamente em 4 testes novos. Garante consistência semântica entre detecção e cálculo.

### 3.2 MET-004B-001 (metodológico) — Opção A

> **GPT:** "O plano lista como mitigação auditar/adaptar expectations em `integration.test.ts`, `bom.test.ts` e `pipeline-diagnostics.test.ts`, mas o escopo permitido de testes menciona apenas `src/lib/layout/__tests__/pressure-class.test.ts`. Se alterações nesses testes forem necessárias, o escopo da task deve ser explicitamente ampliado antes da implementação."

**Resolução adotada:** Opção A — escopo de testes ampliado para incluir esses 3 arquivos **caso necessário**. **Resultado:** auditoria após implementação confirmou que **nenhum desses testes precisou de adaptação** — a propagação dos 2 campos novos (`cumPrincipalHfM`, `adutoraHfM`) é aditiva e os testes existentes nesses arquivos não validam classificação de PN em ramais/laterais com derivação. Resultado vitest: 826 → 836 (apenas os 10 novos T04B + 4 testes do helper).

---

## 4. Mudanças aplicadas

### 4.1 `src/lib/layout/hydraulic-sizing.ts` (5 edits cirúrgicos)

| # | Localização | Mudança |
|---|---|---|
| 1 | Interface `HydraulicSegment` (linha ~126-141) | Adicionados 2 campos opcionais: `cumPrincipalHfM?: number` e `adutoraHfM?: number` com JSDoc explicando que são populados apenas para `secondary`/`lateral` quando o solver propaga os valores |
| 2 | Interface `HydraulicModelLimitations.pressureClassModel` (linha ~81-93) | Tipo muda de literal `"hmt_conservative_inlet"` para união `"hmt_conservative_inlet" \| "exact_per_derivation"`; JSDoc atualizado descrevendo os 2 modelos |
| 3 | Função `annotatePressureClass` (linha ~187-243) | Caminho novo para `secondary`/`lateral` quando ambos `cumPrincipalHfM != null && adutoraHfM != null`: calcula pressão real `hmtMca - adutoraHfM - cumPrincipalHfM` e classifica `ok`/`violation_confirmed`. Fallback legado preservado (sem ambos campos → comportamento `violation_conservative` original). Novo helper exportado `derivePressureClassModel` adicionado em seguida |
| 4 | Construção de `sSecSegs` e `sLatSegs` no solver (linha ~518-533 e ~545-561) | Popula os 2 novos campos com `cumPrincipalHfM` (destruturado do `EnrichedSeg` em escopo) e `adutoraHfM: adutoraHf` (variável do laço por setor em escopo) |
| 5 | Montagem de `modelLimitations` (linha ~882) | `pressureClassModel: "hmt_conservative_inlet"` substituído por `pressureClassModel: derivePressureClassModel(allSegs)` — detecção dinâmica |

**Linhas:** 838 → ~866 (+28 linhas). Solver, HMT, validação de bomba, caminho crítico **não tocados**.

### 4.2 `src/lib/layout/__tests__/pressure-class.test.ts`

| # | Localização | Mudança |
|---|---|---|
| 1 | Import (linha 2) | Adiciona `derivePressureClassModel` aos imports |
| 2 | Helper `makeSeg` (linha 13-32) | Ganha parâmetro opcional `derivation?: { cumPrincipalHfM: number; adutoraHfM: number }`; spread aplica os campos quando passado |
| 3 | Novo describe block (após T15) | "TASK-004B — annotatePressureClass com pressão real por derivação" — 6 testes T04B-1..T04B-6 |
| 4 | Novo describe block | "TASK-004B — derivePressureClassModel" — 4 testes do helper |

**Linhas:** 242 → 365 (+123 linhas). Helper antigo retrocompatível: testes T01..T15 pré-existentes continuam passando **sem alteração**.

### 4.3 Outros arquivos modificados (governança/docs)

- `tasks/TASK-004B-pressao-real-derivacao.md` — criado (task file formal)
- `tasks/backlog.md` — header atualizado + entrada nova TASK-004B + ajuste pendência da TASK-004 mãe (linha 118)
- `docs/relatorios/2026-05-22-TASK-004B.md` — este relatório
- `ai/current-task.md` — ciclo TOOL-003
- `ai/claude-report.md` — plano TASK-004B serializado
- `ai/gpt-review.md` — revisão GPT
- `ai/decision-log.md` — entry append-only TASK-004B

---

## 5. Testes — entregues

| ID | Cenário | Resultado |
|---|---|---|
| **T04B-1** | lateral PN40 com `cumPrincipalHfM=10, adutoraHfM=5, hmt=45` | pressão real **30 mca** ≤ 40 ⇒ `ok` (vs antigo `violation_conservative`) ✅ |
| **T04B-2** | lateral PN40 com `cumPrincipalHfM=2, adutoraHfM=1, hmt=45` | pressão real **42 mca** > 40 ⇒ `violation_confirmed` (blocker real, vs antigo warning) ✅ |
| **T04B-3** | ramal PN80 com `cumPrincipalHfM=5, adutoraHfM=3, hmt=80` | pressão real **72 mca** ≤ 80 ⇒ `ok` ✅ |
| **T04B-4** | ramal sem `cumPrincipalHfM` (fallback legado) | HMT=85 > PN=80 ⇒ `violation_conservative` (preserva comportamento original) ✅ |
| **T04B-5** | sequência adutora(PN80,hf=10) → principal(PN80,hf=5) → ramal(PN80,cum=5,adu=10) → lateral(PN40,cum=5,adu=10) com HMT=60 | adutora 60 ok, principal 50 ok, ramal 45 ok, **lateral 45 violation_confirmed** (45>40) ✅ |
| **T04B-6** | lateral com apenas `cumPrincipalHfM` (falta `adutoraHfM`) | Fallback ativa (Opção A do ajuste TEC-004B-001) → `violation_conservative` ✅ |
| **T04B-DerivM-1** | `derivePressureClassModel` sem ramais/laterais | retorna `"hmt_conservative_inlet"` ✅ |
| **T04B-DerivM-2** | `derivePressureClassModel` com TODOS ramais/laterais com ambos campos | retorna `"exact_per_derivation"` ✅ |
| **T04B-DerivM-3** | `derivePressureClassModel` com algum sem ambos os campos | retorna `"hmt_conservative_inlet"` (fallback) ✅ |
| **T04B-DerivM-4** | `derivePressureClassModel` com ramal só com `cumPrincipalHfM` (falta `adutoraHfM`) | retorna `"hmt_conservative_inlet"` (Opção A) ✅ |

**Suíte vitest completa:** 826 → **836 passando** (41 arquivos; sem regressão; 1,48s).

**Testes preservados:** 15 testes T01..T15 pré-existentes em `pressure-class.test.ts` continuam passando sem alteração.

---

## 6. Critérios de aceite — auditoria

Todos os 25 critérios listados no plano `/planejar TASK-004B` cumpridos:

- [x] `HydraulicSegment` ganha campos opcionais `cumPrincipalHfM?` e `adutoraHfM?`
- [x] `HydraulicModelLimitations.pressureClassModel` aceita 2 valores
- [x] `annotatePressureClass` calcula pressão real quando dados disponíveis
- [x] Fallback preservado: sem ambos os campos → `violation_conservative`
- [x] Helper `derivePressureClassModel` exportado e testado isoladamente
- [x] `sSecSegs` e `sLatSegs` populam os novos campos
- [x] `modelLimitations.pressureClassModel` detectado dinamicamente
- [x] 6 testes T04B-1..T04B-6 + 4 testes do helper passando (10 totais)
- [x] 15 testes T01..T15 pré-existentes continuam passando sem alteração
- [x] `tasks/backlog.md` atualizado
- [x] `tasks/TASK-004B-*.md` criado
- [x] `docs/relatorios/2026-05-22-TASK-004B.md` criado
- [x] **Mapa Mestre NÃO alterado**
- [x] **Premissas RT/campo NÃO alteradas**
- [x] **ADR-008 NÃO alterada** (sem emenda; sem ADR-016)
- [x] **`01-regras-bloqueantes.md` NÃO alterado** (sem RB-09)
- [x] **`03-hidraulica.md` NÃO alterado**
- [x] **Catálogo NÃO alterado** (RB-04)
- [x] **`src/lib/bom.ts` NÃO alterado** (RB-05)
- [x] **`src/lib/pdf/*` NÃO alterado**
- [x] **`src/components/**` NÃO alterado** (RB-06)
- [x] **`src/app/**` NÃO alterado**
- [x] **`src/lib/layout/secondary-sizing.ts` NÃO alterado**
- [x] **Geometria intocada** (RB-08): `laterais.ts`, `sectorization.ts`, `constructability.ts`, `network-angle-diagnostics.ts`, `sprinkler-grid.ts`, `irrigation-project.ts`
- [x] `npx tsc --noEmit` → **0 erros**
- [x] `npx vitest run` → **836/836 passando** (era 826; +10 novos)
- [x] `node scripts/ai/__tests__/run-all.mjs` → 27/27 passando
- [x] Sem commit, sem push (aguarda autorização explícita)

---

## 7. Invariantes — auditoria (7/7 ok)

| Invariante | Status | Justificativa final pós-implementação |
|---|---|---|
| INV-CATALOGO-SEM-HOMOLOGACAO | ok | `src/lib/catalog/aspersores.ts` intocado |
| INV-NAO-INVENTAR-SKU | ok | Sem SKU novo |
| INV-DN100-LATERAL-5022 | ok | ADR-013 preservada; restrição DN homologado inalterada |
| **INV-BLOCKERS-TECNICOS** | **ok** | TASK-004B não relaxa blockers — transforma `violation_conservative` (warning) em `ok` quando pressão real ≤ PN (correção de falso positivo) ou em `violation_confirmed` (blocker NOVO genuíno) quando pressão real > PN (endurecimento). Casos sem dados de derivação preservam fallback `violation_conservative`. Resultado líquido: **mais precisão, sem relaxamento técnico**. |
| INV-MASCARAR-PENDENCIA | ok | Limitação remanescente documentada explicitamente: perdas locais proporcionais e desnível geodético por segmento ficam para task futura separada |
| INV-DOMINIO-FORA-UI | ok | Apenas `src/lib/layout/hydraulic-sizing.ts` e seus testes; sem UI |
| INV-LAYOUT-INSTAVEL-COMERCIAL | ok | Não avança comercial; melhora precisão técnica do diagnóstico de PN |

---

## 8. Limitações conhecidas (V1) — não regrediram

| Item | Status |
|---|---|
| **Telemetria de custo declarada pelo modelo** (limitação V1 herdada de TOOL-002/003) | mantida — `tokens_prompt=0`, `tokens_completion=0`, `custo_estimado_usd=0` no JSON; referência real = dashboard OpenAI |
| **Sem cap automático de custo** (`OPENAI_COST_CAP_USD`) | mantida — cap humano/manual ≤ US$ 0,50 por execução |
| **Sem retry automática** em caso de HTTP ≠ 200 | mantida — `/gpt-review` aborta com stderr |
| **Perdas locais proporcionais não incluídas** na fórmula de pressão real | **nova nesta task** — fórmula nível mínimo `HMT − adutoraHfM − cumPrincipalHfM` é ligeiramente conservativa (omite redução por perdas locais ~10%); preserva INV-BLOCKERS-TECNICOS (erra para o lado seguro); task futura pode promover para fórmula nível médio |
| **Desnível geodético por segmento** | **mantida** — pendência irmã da TASK-004 mãe; explicitamente fora do escopo da TASK-004B; task futura separada |

Nenhuma dessas limitações compromete INV-MASCARAR-PENDENCIA — todas estão visíveis no relatório, no task file e nos comentários do código.

---

## 9. Verificações finais

```
$ npx tsc --noEmit
EXIT=0

$ npx vitest run
Test Files  41 passed (41)
Tests       836 passed (836)
Duration    1.48s

$ node scripts/ai/__tests__/run-all.mjs
# tests 27 / # pass 27 / # fail 0

$ node scripts/ai/validate-structure.mjs --task TASK-004B
status_atual: em_implementacao
status_sugerido: aguardando_fechamento
transicao_valida: true
override_permitido_derivado: null
WARN: gpt-review.json: override_permitido declarado=true mas derivado=null
OK
```

---

## 10. Arquivos criados / modificados

### 10.1 Criados (3)

- `tasks/TASK-004B-pressao-real-derivacao.md` — task file formal
- `docs/relatorios/2026-05-22-TASK-004B.md` — este relatório

### 10.2 Modificados (8)

- `src/lib/layout/hydraulic-sizing.ts` — 5 edits cirúrgicos (~28 linhas adicionadas)
- `src/lib/layout/__tests__/pressure-class.test.ts` — helper retrocompatível + 10 testes novos (+123 linhas)
- `tasks/backlog.md` — header atualizado + entrada nova + ajuste pendência TASK-004
- `ai/current-task.md` — ciclo TOOL-003
- `ai/claude-report.md` — plano TASK-004B serializado
- `ai/gpt-review.md` — revisão GPT
- `ai/decision-log.md` — entry append-only TASK-004B

### 10.3 Não tocados (escopo proibido respeitado)

- `src/lib/catalog/aspersores.ts` (RB-04)
- `src/lib/bom.ts` (RB-05)
- `src/lib/pdf/*` (PDF intocado)
- `src/components/**`, `src/app/**` (RB-06)
- `src/lib/layout/secondary-sizing.ts` (não cuida de PN no segmento)
- `src/lib/layout/laterais.ts`, `sectorization.ts`, `constructability.ts`, `network-angle-diagnostics.ts`, `sprinkler-grid.ts`, `irrigation-project.ts` (geometria — RB-08)
- `docs/metodologia/12-premissas-provisorias-e-revisao-rt.md` (premissas inventariadas, não alteradas)
- `docs/metodologia/01-regras-bloqueantes.md` (sem RB-09)
- `docs/metodologia/03-hidraulica.md`
- `docs/decisoes/ADR-008-validacao-pn-classe-pressao-tubos.md` (sem emenda; sem ADR-016)
- `tasks/TASK-024-mapa-mestre-tasks.md` (Mapa Mestre fonte)
- `ARQUITETURA_ATUAL.md`, `AGENTS.md`, `CLAUDE.md`
- `scripts/`
- `.claude/commands/*`

---

## 11. Próximos passos sugeridos

| Pri | Item | Tipo | Dependência |
|---|---|---|---|
| 1 | **Humano aprova ou rejeita commit/push** desta task | Operação | — |
| 2 | **Desnível geodético por segmento** | Classe A — motor hidráulico | Pendência irmã da TASK-004 mãe; sem dependência externa |
| 3 | **Comparação HMT motor vs cálculo manual RT** em projeto histórico | Classe E — validação de campo | RT designado + projeto histórico disponibilizado (passos 3 do roteiro TASK-024 §11.2) |
| 4 | **TOOL-004 — captura `response.usage` real** | Classe A — tooling | Independente |
| 5 | **Promoção do épico E03** no Mapa Mestre de "Testado em código" para "Validado em projeto histórico" | Governança | Após passo 3 acima |

**TASK-002 (Motor de Governança A/B/C)** permanece bloqueada por homologação RT do `docs/metodologia/09-classificacao-de-projetos.md` (Blocker B2 do diagnóstico TASK-001 Seção 11), independente desta task.

---

## 12. Rastreabilidade

- **Predecessor:** [TASK-004 — Validar PN/classe de pressão por trecho](2026-05-19-TASK-004.md) (concluída 2026-05-19, ADR-008 — Alternativa C reservada).
- **Diagnóstico que orienta:** [TASK-001 — Diagnóstico formal do software atual](2026-05-22-TASK-001.md) (concluída 2026-05-22, commit `427539e`) — Seção 12.2 #1 identificou esta como próxima Classe A executável; R8 da Seção 9 alerta para o risco mitigado por esta task.
- **ADR de referência:** [`ADR-008`](../../docs/decisoes/ADR-008-validacao-pn-classe-pressao-tubos.md) — Alternativa C explicitamente reservada; entregue por esta task sem necessidade de emenda.
- **Task file:** [`tasks/TASK-004B-pressao-real-derivacao.md`](../../tasks/TASK-004B-pressao-real-derivacao.md).
- **Mapa Mestre §8 #4:** identificou esta task como próxima recomendada da fila.
- **Decision log:** [`ai/decision-log.md`](../../ai/decision-log.md) — entry 2026-05-22T21:02:18-03:00.
- **GPT review:** [`ai/gpt-review.md`](../../ai/gpt-review.md) — hash `35c7ba49c15e9ad477581042833ff964b82eda9619adeb7803c5bb87d9e59efa`.
