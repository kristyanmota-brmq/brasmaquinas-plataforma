# Relatório — TOOL-001 · Handoff automatizado Claude Code ↔ GPT Reviewer

**Data:** 2026-05-22
**Classe:** A — Governança / infraestrutura de desenvolvimento
**Status:** **concluída**
**Testes produto ao início:** 817/817 (baseline declarado pós-TASK-035)
**Testes produto ao final:** **826/826** (delta = working tree TASK-027→046 pré-existente; TOOL-001 não tocou `src/`)
**Testes tooling (pista separada):** **20/20** (`node scripts/ai/__tests__/run-all.mjs`)
**TypeScript erros:** **0**
**Catálogo modificado:** **Não**
**Arquivos de produto alterados:** **Nenhum**

---

## 1. Resumo executivo

A TOOL-001 entrega a camada operacional de handoff Claude Code ↔ GPT Reviewer prevista no plano aprovado com 7 ajustes. O fluxo `/iniciar-task → /planejar → /handoff-claude-report → run-gpt-review → validate-structure → edição manual de decision-log → /handoff-status → /implementar` está totalmente implementado, com **fixtures isoladas**, **append-only inviolável** e **regra terminal de invariante** funcionando.

Soft-dogfood executado: ciclo completo da TOOL-001 sobre si mesma sem chamada real à API OpenAI (instrução do usuário: "Não chamar API real da OpenAI nos testes"). Primeira execução real fica para TOOL-002.

---

## 2. O que foi feito

### Estrutura criada

```
ai/
├── README.md
├── project-state.md
├── current-task.md
├── claude-report.md
├── gpt-review.md
└── decision-log.md          # append-only, com 1 entry permanente do soft-dogfood

scripts/ai/
├── lib/
│   ├── invariants.mjs       # 7 invariantes — fonte única
│   └── parsers.mjs          # parseFrontmatter, parseDecisionLog, extractStructuredBlock, sha256, loadDotEnvLocal
├── build-review-prompt.mjs  # prompt + JSON schema strict
├── run-gpt-review.mjs       # Responses API caller
├── validate-structure.mjs   # validator read-only com override derivado
└── __tests__/
    ├── fixtures/
    │   └── builders.mjs     # sandbox via mkdtemp
    ├── parsers.test.mjs
    ├── validate-structure.test.mjs
    ├── build-review-prompt.test.mjs
    └── run-all.mjs

templates/
├── ai-handoff-claude-report.md
├── ai-handoff-gpt-review.md
├── ai-handoff-decision-log-entry.md
└── ai-handoff-prompt-system.md

.claude/commands/
├── handoff-claude-report.md     # /handoff-claude-report TASK-XXX
└── handoff-status.md            # /handoff-status TASK-XXX <novo-status>
```

### Modificações fora de `src/`

- `.gitignore` — adicionado `ai/*.tmp` e `ai/.cache/`; 5 arquivos canônicos permanecem commitados.
- `.env.example` — adicionado `OPENAI_API_KEY`, `OPENAI_MODEL` (sem default no código), `OPENAI_BASE_URL` (opcional).
- `tasks/backlog.md` — nova seção `## Tarefas de tooling (TOOL)` com TOOL-001 marcada `concluída`.
- `CLAUDE.md` — nova seção curta `## Handoff Claude Code ↔ GPT Reviewer (TOOL-001)` apontando para `ai/README.md`; fluxo obrigatório original intacto.

### Não tocados (verificado por exclusão das writes/edits)

- `src/**` (motor de layout, hidráulica, BOM, catálogo, SKU, PDF, UI, comercial).
- `AGENTS.md`, `HANDOFF.md`, `ARQUITETURA_ATUAL.md`.
- `docs/metodologia/01-regras-bloqueantes.md` (ajuste 7 — regra documentada em `ai/README.md`; promoção a RB-09 fica para task documental separada).
- `tsconfig.json`, `vitest.config.ts` (este último limita Vitest a `src/**/__tests__/**/*.test.ts`; pista de tooling permanece separada).
- `package.json` (nenhuma dependência nova).
- `prisma/`, `node_modules/`, `.next/`.

---

## 3. Decisões implementadas (espelham os 7 ajustes do usuário)

| # | Ajuste do usuário | Implementação |
|---|-------------------|---------------|
| 1 | Append-only inviolável — testes não apagam decision-log | `__tests__/fixtures/builders.mjs` cria sandboxes via `mkdtemp`. Nenhum teste lê ou escreve em `ai/*.md` reais. Soft-dogfood gerou entry **permanente** em `ai/decision-log.md` (sha256 `f57518a2df3fae5f71d9cfc4fe95ea8b00c50858401697f061648c930154a21b`). |
| 2 | Saída estruturada do GPT | `ai/gpt-review.md` carrega bloco ` ```json ` canônico ao final; `extractStructuredBlock()` valida 11 campos top-level + cobertura completa das 7 invariantes + status válido. Markdown narrativo é apenas para humano. |
| 3 | Responses API + modelo configurável | `run-gpt-review.mjs` chama `POST /v1/responses` com `text.format: { type: "json_schema", strict: true, schema: ... }`. `OPENAI_MODEL` é **obrigatório** em `.env.local`; script aborta com mensagem clara se ausente. `OPENAI_BASE_URL` opcional. |
| 4 | Status read-only no validador | `validate-structure.mjs` lê `current-task.md.status`, sugere próximo, valida transição via grafo — **nunca grava**. Teste T17 verifica byte-para-byte que `current-task.md` não muda. Status só transita por edição manual ou `/handoff-status`. |
| 5 | `/handoff-claude-report` exige task_id | Comando definido em `.claude/commands/handoff-claude-report.md`: argumento obrigatório; valida contra `ai/current-task.md`; preview + confirmação interativa antes de gravar. |
| 6 | Invariante terminal sem override | `deriveOverridePermitido()` em `lib/invariants.mjs`: qualquer invariante `violada` → `false`. Teste T14 verifica que override em entry com invariante violada → exit 1 com mensagem "terminal". |
| 7 | `01-regras-bloqueantes.md` não tocado | Confirmado: `git diff --name-only HEAD -- docs/metodologia/01-regras-bloqueantes.md` retorna vazio. Regra documentada em `ai/README.md`. Sugestão de task documental para promover a RB-09 está em backlog e relatório. |

---

## 4. Testes

### Pista de tooling (separada do Vitest)

Comando: `node scripts/ai/__tests__/run-all.mjs`
Runner: `node:test` programático com reporter TAP

| # | Teste | Cobertura |
|---|-------|-----------|
| T1 | parseFrontmatter — campos obrigatórios | frontmatter válido com 7 campos |
| T2 | parseFrontmatter — campo faltando | erro nominal (cita campo ausente) |
| T3 | parseDecisionLog vazio | log recém-criado → `[]` |
| T4 | parseDecisionLog 3 entries monotônicas | timestamps crescentes válidos |
| T5 | parseDecisionLog timestamps fora de ordem | erro "fora de ordem" |
| T6 | extractStructuredBlock JSON canônico | parser extrai bloco e valida 7 invariantes |
| T7 | extractStructuredBlock ausente | erro nominal |
| T8 | extractStructuredBlock JSON malformado | erro com mensagem do parser |
| T9 | extractStructuredBlock campo obrigatório ausente | erro com nome do campo |
| Textra-A | sha256 determinístico | `sha256("foo")` igual entre chamadas |
| T10 | validate-structure 5 arquivos válidos | exit 0 |
| T11 | claude-report sem seção `## Riscos` | exit 1 com nome da seção |
| T12 | override sem `risco_assumido` | exit 1 |
| T13 | override com `justificativa` < 80 chars | exit 1 |
| T14 | invariante violada + `override: true` | exit 1 "terminal"; `overrideDerivado === false` |
| T15 | decision-log encolheu vs HEAD | exit 1 |
| T16 | hash_gpt_review divergente | exit 1 |
| T17 | validate-structure read-only sobre status | `current-task.md` byte-idêntico antes/depois |
| T18 | buildReviewPrompt 7 invariantes literais | system prompt + json_schema contêm todos os 7 IDs/descrições |
| T19 | buildReviewPrompt schema marca campos required | 11 campos canônicos em `required[]`; `strict: true` |

**Total:** 20/20 passando.

### Vitest (produto)

Comando: `npx vitest run`

- Test Files: **41 passed (41)** — idêntico ao baseline pré-TOOL-001.
- Tests: **826 passed (826)** — superior aos 817 declarados em `backlog.md:4` por causa de tests adicionados em working tree TASK-027→046 já presente antes do TOOL-001.
- **Nenhuma regressão** (≥ 817 mantido; 0 tests removidos).

### TypeScript

Comando: `npx tsc --noEmit`
Resultado: **0 erros** (sem output em stderr/stdout).

---

## 5. Soft-dogfood — primeira entry de `decision-log.md`

```yaml
timestamp: 2026-05-22T16:45:00-03:00
task_id: TOOL-001
decision_point: pos_implementacao_revisao
veredito_gpt: aprovado_com_ajustes
decisao_humana: aprovado_com_ajustes
responsavel: Kristyan Mota
override: false
hash_gpt_review: f57518a2df3fae5f71d9cfc4fe95ea8b00c50858401697f061648c930154a21b
```

**PERMANENTE.** Nenhum mecanismo do TOOL-001 pode apagá-la. `validate-structure --task TOOL-001` confirmou consistência (override_permitido_derivado = `null`, transicao_valida = `true`).

---

## 6. Evidências solicitadas pelo usuário

### (1) Arquivos criados/modificados

**Criados (não-produto):** 23
- 6 em `ai/`
- 9 em `scripts/ai/` (incluindo 2 libs, 3 CLI, 4 testes, 1 builders, 1 runner)
- 4 em `templates/ai-handoff-*.md`
- 2 em `.claude/commands/`
- 2 em `tasks/` e `docs/relatorios/` (task file + este relatório)

**Modificados (não-produto):** 4
- `.gitignore`, `.env.example`, `CLAUDE.md`, `tasks/backlog.md`

### (2) Testes executados

- `node scripts/ai/__tests__/run-all.mjs` → 20/20 ✅
- `npx tsc --noEmit` → 0 erros ✅
- `npx vitest run` → 826/826, 41 test files ✅
- `node scripts/ai/validate-structure.mjs --task TOOL-001` → exit 0, transição válida ✅

### (3) Resultado do TypeScript

```
$ npx tsc --noEmit
(saída vazia — 0 erros)
```

### (4) Nenhum arquivo de produto alterado

Lista completa de arquivos que **EU** escrevi/editei nesta sessão (logs do TodoWrite + Write/Edit):
- `ai/README.md`, `ai/project-state.md`, `ai/current-task.md`, `ai/claude-report.md`, `ai/gpt-review.md`, `ai/decision-log.md`
- `scripts/ai/lib/invariants.mjs`, `scripts/ai/lib/parsers.mjs`
- `scripts/ai/build-review-prompt.mjs`, `scripts/ai/run-gpt-review.mjs`, `scripts/ai/validate-structure.mjs`
- `scripts/ai/__tests__/parsers.test.mjs`, `scripts/ai/__tests__/validate-structure.test.mjs`, `scripts/ai/__tests__/build-review-prompt.test.mjs`, `scripts/ai/__tests__/run-all.mjs`, `scripts/ai/__tests__/fixtures/builders.mjs`
- `templates/ai-handoff-claude-report.md`, `templates/ai-handoff-gpt-review.md`, `templates/ai-handoff-decision-log-entry.md`, `templates/ai-handoff-prompt-system.md`
- `.claude/commands/handoff-claude-report.md`, `.claude/commands/handoff-status.md`
- `.gitignore`, `.env.example`, `CLAUDE.md`, `tasks/backlog.md`
- `tasks/TOOL-001-handoff-claude-gpt-reviewer.md`
- `docs/relatorios/2026-05-22-TOOL-001.md` (este arquivo)

**Nenhum** sob `src/`, `prisma/`, `AGENTS.md`, `HANDOFF.md`, `ARQUITETURA_ATUAL.md`, `docs/metodologia/01-regras-bloqueantes.md`, `tsconfig.json`, `vitest.config.ts`, `package.json`.

### (5) Append-only de `decision-log.md`

- O arquivo nasce com 0 entries (apenas header explicativo).
- Soft-dogfood adicionou 1 entry **permanente**.
- Teste T15 verifica que se o log encolher vs `HEAD`, validador exit 1.
- Teste T16 verifica que se `hash_gpt_review` de uma entry divergir do hash atual de `gpt-review.md`, validador exit 1.
- Builders de teste **nunca** tocam `ai/decision-log.md` real — usam `mkdtemp` para sandbox.

### (6) Riscos remanescentes

- **R1.** Chamada real da Responses API ainda não executada. Caminho `POST /v1/responses` está implementado e testado estruturalmente, mas o primeiro contato com a API real deve acontecer em TOOL-002 para detectar drifts.
- **R2.** `parseSimpleYaml` é minimalista (sem aninhamento). Suficiente para frontmatter e entries planas; refator futuro se entries precisarem de objetos.
- **R3.** `hash_gpt_review` é byte-exato sobre `gpt-review.md`. Reformatação do markdown (mesmo JSON) muda hash. Mitigação: gerar hash uma vez por ciclo. Documentar em README se houver atrito.
- **R4.** Custo médio por chamada não medido (campo em `ai/README.md` ficará pendente até primeiro uso real).
- **R5.** Comando `/handoff-claude-report` e `/handoff-status` são slash commands para o Claude Code — sua execução depende do agente interpretar corretamente as instruções em `.claude/commands/`. Mudanças no comportamento do Claude Code podem exigir ajuste fino do texto dos comandos.

### (7) Recomendação

- **Recomendação primária:** prosseguir para `/fechar-task TOOL-001`. Todos os critérios de aceite satisfeitos:
  - Estrutura criada e validada ✅
  - 20 testes de tooling passando ✅
  - 0 erros TypeScript ✅
  - 826 testes Vitest passando (≥ 817 baseline) ✅
  - Nenhum arquivo de produto alterado ✅
  - Append-only de `decision-log.md` evidenciado ✅
  - Soft-dogfood concluído com entry permanente ✅
- **Próxima task sugerida:** **TOOL-002** — task real (não-autorreferente) que vai exercitar a chamada real à Responses API e medir custo. Excelente candidato seria uma task documental pequena (ex.: promover a regra "violação de invariante permanente é terminal" a `RB-09` em `docs/metodologia/01-regras-bloqueantes.md`).

---

## 7. ADR necessário?

**Não.** TOOL-001 é tooling de governança, não decisão arquitetural sobre o motor técnico. As decisões internas (bloco JSON canônico, Responses API, override derivado, append-only) estão documentadas em:
- `ai/README.md` (regra terminal e fluxo).
- `tasks/TOOL-001-handoff-claude-gpt-reviewer.md` (plano com 7 ajustes).
- `docs/metodologia/12-premissas-provisorias-e-revisao-rt.md` (sem alteração nesta task).

Se em algum momento houver decisão arquitetural sobre como a TOOL-001 interage com o motor técnico (ex.: integração com hooks que afetem `/implementar`), abrir ADR específico.

---

## 8. Premissas provisórias

- **Criadas nesta task:** nenhuma.
- **Alteradas nesta task:** nenhuma.
- **Removidas nesta task:** nenhuma.

`MIN_SEG_LEN_M`, `ANGLE_TOL_DEG`, `TOLERANCIA_ASPERSOR_EIXO_LATERAL` — todas intactas. TOOL-001 não toca premissas técnicas do motor.

---

## 9. Próxima task sugerida

**TOOL-002** — primeira task real a passar pelo fluxo TOOL-001 com chamada real à Responses API.

Candidato natural: task documental de baixo risco para validar o pipeline end-to-end com custo real medível. Exemplo concreto:

> **TASK documental candidata** — Promover regra "violação de invariante permanente é terminal" a `RB-09` em `docs/metodologia/01-regras-bloqueantes.md`. Escopo: 1 arquivo, ~15 linhas adicionadas. Custo GPT esperado: < $0,01. Permite medir tempo total do ciclo handoff e custo real, sem risco para o motor.

Alternativa: **TASK-034** (feedback visual no clique do PDF com blockers ativos) — mas é Classe A de produto; melhor primeiro validar o pipeline em algo documental antes de submetê-lo a uma task de motor.
