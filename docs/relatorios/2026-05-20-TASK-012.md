# Relatório de implementação — TASK-012
## Saneamento de working tree e separação de commits

**Data:** 2026-05-20
**Autor:** Claude Sonnet 4.6
**Status:** Concluída

---

## Objetivo

Organizar o estado acumulado do repositório — 17 tracked modificados e 42 untracked — em commits limpos por responsabilidade, sem alterar nenhum arquivo de código.

---

## Estado dos testes

| Momento | Passando | Total | TypeScript |
|---------|----------|-------|------------|
| Início  | 597      | 597   | 0 erros    |
| Fim     | 597      | 597   | 0 erros    |

Nenhum arquivo em `src/` foi alterado durante esta etapa documental.

---

## Commits criados (10)

| Commit | Mensagem | Conteúdo |
|--------|----------|---------|
| `9cbd5fb` | `chore: ignore local workspace artifacts` | `.gitignore` (+7 linhas) |
| `62e967f` | `chore: add @react-pdf/renderer dependency` | `package.json`, `package-lock.json` |
| `8f11696` | `docs: add CLAUDE.md governance content and claude commands` | `CLAUDE.md`, `.claude/commands/` (6 md) |
| `276750a` | `docs: add ARQUITETURA_ATUAL.md architecture snapshot` | `ARQUITETURA_ATUAL.md` |
| `1517939` | `feat: add layout domain core modules and physical column model` | 31 arquivos (ver abaixo) |
| `e245979` | `feat: add offset grid variant for layout candidate generation (TASK-010B)` | `sprinkler-grid.ts`, `sprinkler-grid.test.ts` |
| `ecbb459` | `feat: add invalid hydraulic segments diagnostic report` | `irrigation-project.ts`, `pdf/route.tsx` |
| `944f5db` | `refactor: import ProjectLayout from layout-schema module` | `actions.ts`, `page.tsx` |
| `e954d71` | `docs: add ADR policy and retroactive ADRs 001-008 (TASK-011)` | `arquitetura.md`, 8 ADRs, relatório, task file |
| `8afaa00` | `docs: add historical reports and task files` | 2 relatórios + 8 task files |

---

## Detalhe do commit de fundação (`1517939` — 31 arquivos, 8921 inserções)

### Módulos de domínio novos (untracked)

| Arquivo | O que faz |
|---------|-----------|
| `pipeline-types.ts` | Tipos de geometria de pipeline compartilhados |
| `sectorization.ts` | Setorização por vazão com split de coluna física |
| `hydraulic-graph.ts` | Grafo de adjacência para verificação de conectividade |
| `secondary-sizing.ts` | Dimensionamento individual de ramais |
| `hydraulic-connectivity.ts` | Mapeamento ramal → inlet da coluna física |
| `constructability.ts` | Verificações de construtibilidade (entrada, overlap, colunas curtas) |
| `pipeline-diagnostics.ts` | Diagnósticos geométricos de pipeline |
| `map-consistency.ts` | Consistência visual/topológica do mapa |
| `layout-use-cases.ts` | Helpers de orquestração de layout |
| `pdf/PropostaPDF.tsx` | Template de proposta técnica em PDF |

### Tracked modifications fundidas (dependência circular sectorization ↔ laterais)

| Arquivo | O que mudou |
|---------|------------|
| `laterais.ts` | `PhysicalColumn`, `generatePhysicalColumns`, `selectLateralTube` (gate velocidade + diâmetro interno) |
| `principal.ts` | Assinatura `Lateral[]` → `PhysicalColumn[]` |
| `hydraulic-sizing.ts` | `HYDRAULIC_LIMITS` exportado, campo `internalDiameterMm` em `HydraulicSegment` |
| `aspersores.ts` | `selectTubo` usa `diametroInternoMm` (ADR-002) |
| `hazenWilliams.ts` | Ajuste menor |

### Arquivos de teste (14 novos + 2 atualizados)

`fixtures`, `bom`, `constructability`, `hydraulic-connectivity`, `hydraulic-sizing`,
`hydraulic-sizing-diagnostics`, `integration`, `laterais-velocity-gate`, `map-consistency`,
`pipeline-diagnostics`, `principal-velocity-gate`, `secondary-sizing`, `sectorization`,
`sectorization-split` (novos) + `laterais.test.ts`, `principal.test.ts` (atualizados).

---

## Arquivos ignorados (adicionados ao .gitignore)

| Arquivo / diretório | Motivo |
|--------------------|--------|
| `.vscode/` | Configuração local de IDE |
| `memory/` | Armazenamento interno do Claude — não pertence ao repositório |
| `HANDOFF.md` | Arquivo operacional volátil; fonte de verdade são relatórios e backlog |
| `update_catalog_and_bom.py` | Script one-off de análise; fora do escopo do codebase |
| `.claude/settings.local.json` | Configuração local de sessão Claude |

---

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---------|----------------------|--------|
| Fundir commits 3+4+5+7 do plano em um único commit de fundação | Commits separados por responsabilidade | Dependência circular: `sectorization.ts` importa `PhysicalColumn` de `laterais.ts`, que (no diff) importa `OperationalSegment` de `sectorization.ts`. Nenhum compila isoladamente sem o outro. |
| `ARQUITETURA_ATUAL.md` commitado como snapshot | Não commitar | Documento de referência histórica válido; "nunca alterar" = não modificar durante tasks, não = não versionar. |
| `HANDOFF.md` adicionado ao `.gitignore` | Commitar | Arquivo operacional volátil; fonte de verdade são `docs/relatorios/`, `tasks/backlog.md` e ADRs. |

---

## Invariantes verificadas

- [x] `npx tsc --noEmit` → 0 erros
- [x] `npx vitest run` → 597/597 passando
- [x] Orquestrador único preservado
- [x] Nenhuma lógica de domínio introduzida em UI
- [x] Catálogo imutável
- [x] `memory/` não commitado
- [x] `.vscode/` não commitado
- [x] `.claude/commands/*.md` commitado; `settings.local.json` ignorado
- [x] Nenhum arquivo em `src/` alterado nesta etapa documental

---

## Pendências abertas

- [ ] **Commits históricos (pré-2a5e451)** referenciam arquivos que agora estão rastreados mas antes não estavam. O histórico anterior a esta sessão contém commits tecnicamente incompletos se alguém fizer checkout neles isoladamente. Decisão: aceitar como trade-off — o repositório está limpo a partir de agora.
- [ ] **`update_catalog_and_bom.py`** permanece no working tree (ignorado). Avaliar se deve ser deletado definitivamente.
- [ ] **Calibração RT — OPTIMIZER_PARAMS** — todos os pesos do motor de candidatos continuam `PENDENTE_CALIBRACAO_RT_CAMPO` ou `PENDENTE_REVISAO_RT_BRASMAQUINAS`.

---

## Próximos passos sugeridos

1. **Labels de setor no mapa** — marcadores devem usar `PhysicalColumn.startLngLat`, não o centroide (`ProjectMap.tsx`, bloco `sectorLabelsGeoJSON`)
2. **Diâmetro dos ramais no PDF** — `PropostaPDF.tsx` não exibe SKU individual por ramal; incluir coluna de `sizedSecondaries`
3. **Calibração RT** — apresentar `OPTIMIZER_PARAMS` ao RT da Brasmáquinas para validação dos pesos provisionais
